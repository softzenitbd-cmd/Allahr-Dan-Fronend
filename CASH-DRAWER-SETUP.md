# ক্যাশ ড্রয়ার সেটআপ / Cash Drawer Setup

দোকানের টাকার বাক্সটা (ক্যাশ ড্রয়ার) কম্পিউটারের সাথে লাগানো থাকে না — লাগানো থাকে
**রসিদ প্রিন্টারের পেছনের RJ11 সকেটে** (দেখতে টেলিফোনের তারের মতো)। প্রিন্টার যখন
কিছু ছাপে, তখনই সে ড্রয়ারটা খুলে দেয়।

ব্রাউজার নিজে ড্রয়ার খুলতে পারে না — ওয়েব পেজকে USB বা সিরিয়াল পোর্টে যেতে দেওয়া হয় না।
তাই সফটওয়্যার যা করে: **প্রিন্টারে ছাপার কাজ পাঠায়**, আর প্রিন্টার ড্রয়ার খোলে।

এর জন্য প্রিন্টারে **একবার** একটা সেটিং চালু করতে হবে।

---

## এক বারের সেটআপ (Windows)

1. **Control Panel → Devices and Printers** খুলুন
2. আপনার রসিদ প্রিন্টারে **ডান-ক্লিক → Printer properties**
3. **Preferences** বা **Device Settings** ট্যাবে যান
4. খুঁজুন এবং টিক দিন:

   > ☑ **Open cash drawer before printing**
   > (অথবা "Open cash drawer after printing" / "Cash Drawer: Enable")

5. **Apply → OK**

XPrinter, Epson, Rongta — এই কাজের সব থার্মাল প্রিন্টারেই অপশনটা আছে, শুধু নামটা
একটু এদিক-ওদিক হয়।

**এই টিক না দিলে সফটওয়্যারের কোনো কিছুই ড্রয়ার খুলতে পারবে না** — এটাই একমাত্র শর্ত।

---

## এরপর যা হবে

| কাজ | ড্রয়ার |
|---|---|
| বিক্রির পর **Print Receipt** চাপলে | খুলবে |
| POS-এ **Open Drawer** বাটন চাপলে | খুলবে (একটা "NO SALE" স্লিপ ছাপবে) |

`Open Drawer` বাটনটা POS পেজের উপরে ডানদিকে। বিক্রি ছাড়াই ড্রয়ার খোলার দরকার হলে
— আগের কাস্টমারের ভাংতি দিতে, বা সকালে খুচরা টাকা রাখতে — এটা ব্যবহার করবেন।
প্রতিবার একটা ছোট স্লিপ ছাপে যাতে কে কখন বিনা বিক্রিতে ড্রয়ার খুলেছে তার হিসাব থাকে।

---

## কাজ না করলে যা দেখবেন

| সমস্যা | কারণ |
|---|---|
| প্রিন্ট হয় কিন্তু ড্রয়ার খোলে না | উপরের টিকটা দেওয়া হয়নি |
| কিছুই ছাপে না | ব্রাউজারে প্রিন্ট ডায়ালগ বাতিল হয়েছে, বা ভুল প্রিন্টার নির্বাচিত |
| ড্রয়ার একবারও খোলেনি | RJ11 তারটা প্রিন্টারে ঠিকমতো লাগেনি, বা ড্রয়ারে বিদ্যুৎ নেই |

তার ঠিক আছে কিনা দেখতে: প্রিন্টারের সাথে আসা টেস্ট ইউটিলিটি দিয়ে ড্রয়ার খুলে দেখুন।
ওখানে খুললে তার ঠিক আছে, সমস্যা শুধু ড্রাইভারের সেটিংয়ে।

---

## যদি প্রিন্টারে ড্রয়ার লাগানো না থাকে

ড্রয়ারটা যদি আলাদা USB তারে কম্পিউটারে লাগানো থাকে, তাহলে উপরের পথ কাজ করবে না।
সেক্ষেত্রে দুইটা উপায়:

- **Web Serial API** — Chrome থেকে সরাসরি ESC/POS কমান্ড (`1B 70 00 19 FA`) পাঠানো।
  প্রথমবার ইউজারকে পোর্ট বেছে দিতে হয়, HTTPS বা localhost লাগে, শুধু ডেস্কটপ Chrome-এ চলে।
- **ছোট local helper প্রোগ্রাম** — POS কম্পিউটারে একটা সার্ভিস চলবে যেটা কমান্ড পাঠাবে।
  সবচেয়ে নির্ভরযোগ্য, কিন্তু প্রতিটা কম্পিউটারে ইনস্টল করতে হয়।

দরকার হলে এর যেকোনোটা যোগ করা যাবে।

---

## কোড কোথায়

- `src/utils/cashDrawer.js` — ড্রয়ার খোলার ফাংশন ও স্লিপ
- `src/pages/POS.jsx` — "Open Drawer" বাটন

ছাপা হয় একটা লুকানো iframe দিয়ে, তাই ছাপার সময় POS পেজ বা কার্ট নষ্ট হয় না।
